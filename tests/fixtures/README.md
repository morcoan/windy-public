# Test fixtures

`sample.exe` and `complex.exe` are small Windy-authored Windows x64 PE fixtures compiled from the adjacent C sources. They contain no third-party program code and exist only for deterministic parser, analysis, MCP, persistence, and decompiler tests.

The fixtures are not release inputs and are not installed with Windy.
